<%= provide(:title, @user.name) %>
<table class = "table">
	<thead>
		<tr>
			<th>Name</th>
			<th>Email</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td><%= @user.name %></td>
			<td><%= @user.email %></td>
		</tr>
	</tbody>
</table>