<% provide(:title, 'About') %>
<div class="about topspace">
	<div class="col-sm-10">
		<p><%= markdown("
The`Front-End`of this application is based on`Bootstrap 3.3.1` and the whole application works on`Rails 4.1.1`.The result of test which has been performed by`Guard Minitest`is:
```shell
Finished in 5.084775s, 10.0299 runs/s, 21.2399 assertions/s.
51 runs, 108 assertions, 0 failures, 0 errors, 0 skips
```
For testing application:
```shell
# Regular User
Username: example-1@example.com
Password: password
```
```shell
# Admin
Username: example@example.com
Password: foobar
```
Below you can find the list of gems:
```ruby
gem 'rails', '4.1.1'
gem 'pg'
gem 'sass-rails', '4.0.3'
gem 'uglifier', '1.3.0'
gem 'coffee-rails', ' 4.0.0'
gem 'jquery-rails', '3.1.0'
gem 'jquery-ui-rails', '4.2.1'
gem 'turbolinks', '2.3.0'
gem 'jbuilder', '2.0'
gem 'therubyracer'
gem 'less-rails-bootstrap'
gem 'faker'
gem 'random_data'
gem 'simple_form',  github: 'plataformatec/simple_form'
gem 'bcrypt'
gem 'will_paginate'
gem 'redcarpet'
gem 'rouge'

group :development, :test do
gem 'better_errors'
gem 'binding_of_caller'
gem 'spring'
gem 'guard'
gem 'guard-livereload', require: false
gem 'guard-minitest'
end

group :test do
gem 'selenium-webdriver'
end

group :doc do
gem 'sdoc', ' 0.4.0', require: false
end

group :production do
gem 'rails_12factor', '0.0.2'
end
```
") %></p>
	</div>
</div>



